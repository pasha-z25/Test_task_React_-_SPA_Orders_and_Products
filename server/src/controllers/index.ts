import { login, register } from './auth';
import { getOrderById, getOrders } from './orders';
import { getProductById, getProducts } from './products';

export {
  getOrderById,
  getOrders,
  getProductById,
  getProducts,
  login,
  register,
};
